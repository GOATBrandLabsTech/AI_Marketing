# Copy this file to .env and fill in real values. .env is git-ignored.

# Shared password that gates the whole dashboard.
DASHBOARD_PASSWORD=change-me

# Flask session signing key - any random string.
SECRET_KEY=change-me-too

# --- Database ---
# Option A: point at the same cred file your notebooks already use
# (host, user, password, dbname, port - one per line). Leave DB_HOST unset
# to use this instead.
WEBAPP_CRED_FILE=C:\Users\Amit Singh\Documents\Python_Scripts\Voylla_Cred.txt

# Option B: set these directly (used when DB_HOST is present).
# DB_HOST=
# DB_USER=
# DB_PASSWORD=
# DB_NAME=gbl_data_lake
# DB_PORT=5432
